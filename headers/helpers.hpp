#ifndef HELPERS_HPP
#define HELPERS_HPP

#include <QString>

enum class MainWindowProps : int
{
    windowSizeW = 1050,
    windowSizeH = 700,
};

enum class Menus : int
{
    MainMenu,
    SettingsMenu,
    QuitMenu,
    GameWidget,
};

#endif // HELPERS_HPP
